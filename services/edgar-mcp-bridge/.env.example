# EDGAR MCP Bridge Environment Variables

# Required: SEC User Agent (must include your email)
SEC_EDGAR_USER_AGENT="EdgarAnswerEngine/1.0 (your.email@example.com)"

# Bridge server configuration
BRIDGE_PORT=3001
NODE_ENV=development

# MCP Server configuration
MCP_SERVER_HOST=edgar-mcp
MCP_TIMEOUT=30000

# Docker configuration
DOCKER_IMAGE=stefanoamorelli/sec-edgar-mcp:latest